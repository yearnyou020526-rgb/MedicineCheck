<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcher_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/launcher_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medicine">

        <activity
            android:name=".ConfirmUndoActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.Dialog" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".MedicineWidgetProvider"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.medicinecheck.ACTION_MARK_TODAY" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/medicine_widget_info" />
        </receiver>

        <receiver
            android:name=".MedicineCardWidgetProvider"
            android:exported="true"
            android:label="@string/widget_card_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.medicinecheck.ACTION_MARK_TODAY" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/medicine_card_widget_info" />
        </receiver>

        <receiver
            android:name=".MidnightUpdateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".MedicineReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".MedicineMissedReminderReceiver"
            android:exported="false" />
    </application>

</manifest>
